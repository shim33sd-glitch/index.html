<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Our Memory ❤️</title>
    <style>
        body {
            margin: 0; padding: 0; display: flex; justify-content: center; align-items: center;
            height: 100vh; background: linear-gradient(135deg, #ff9a9e, #fecfef);
            font-family: sans-serif; overflow: hidden; text-align: center;
        }
        .container {
            background: rgba(255, 255, 255, 0.5); backdrop-filter: blur(10px);
            padding: 30px; border-radius: 20px; box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            z-index: 10; max-width: 85%;
        }
        /* شكل الصورة */
        .couple-img {
            width: 150px; height: 150px; border-radius: 50%;
            object-fit: cover; border: 5px solid white; margin-bottom: 20px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }
        h1 { color: #d1306e; font-size: 1.8em; }
        .play-btn {
            background: #d1306e; color: white; border: none;
            padding: 15px 30px; border-radius: 50px; cursor: pointer;
            font-weight: bold; margin-top: 20px;
        }
        .heart {
            position: absolute; color: rgba(255, 75, 75, 0.7);
            animation: move 6s linear infinite; top: -10%; z-index: 1;
        }
        @keyframes move { to { transform: translateY(110vh) rotate(360deg); } }
    </style>
</head>
<body>

    <div class="container">
        <img src="photo.jpg" alt="Us" class="couple-img">
        <h1>إلى أجمل إنسانة... ❤️</h1>
        <button class="play-btn" onclick="playMusic()">▶ اسمعي الأغنية</button>
        <audio id="myAudio"><source src="song.mp3" type="audio/mpeg"></audio>
    </div>

    <script>
        function playMusic() {
            document.getElementById("myAudio").play();
            document.querySelector('.play-btn').innerText = "🎵 شغال الآن";
        }
        function createHeart() {
            const heart = document.createElement('div');
            heart.classList.add('heart');
            heart.innerHTML = '❤️';
            heart.style.left = Math.random() * 100 + 'vw';
            heart.style.animationDuration = Math.random() * 3 + 3 + 's';
            document.body.appendChild(heart);
            setTimeout(() => { heart.remove(); }, 6000);
        }
        setInterval(createHeart, 300);
    </script>
</body>
</html>
